# web_fetch

Fetch a **specific URL** (label PDF page, guideline HTML, DrugBank) when openFDA/PubMed lack a number.

- Prefer https. Cap is enforced server-side (~10k chars).
- Cite the URL. Do not invent values from a failed fetch.
